{
  "name": "mcbridge-mcp-server",
  "version": "0.1.0",
  "description": "Serveur MCP (stdio) qui expose le mod client Minecraft 'mcbridge' sous forme de tools : screenshots, camera, focus, options client, reflexion.",
  "type": "module",
  "bin": {
    "mcbridge-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "dev": "node --experimental-strip-types --watch src/index.ts",
    "smoke": "node scripts/smoke.mjs",
    "bundle": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=../plugins/mcbridge/server/mcbridge-mcp.mjs",
    "test": "npm run build && node --test test/",
    "check:contract": "npm run build && node ../scripts/check-contract.mjs",
    "verify": "npm run test && npm run check:contract && npm run smoke"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.7.0"
  }
}
